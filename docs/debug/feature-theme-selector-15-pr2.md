Feature: Theme selector corner menu with 15 themes (PR branch)

This branch contains the Theme Selector implementation (floating corner menu, themes.json, styles, component).

Quick test:
- Open http://localhost:3000
- Click the floating button bottom-right to open the Theme menu
- Select a theme; UI updates immediately and persists across reloads.

Generated and pushed by Copilot-CLI to create a clean PR branch.

# API Contract v1

Der aktuelle API-Contract liegt in:

- `docs/API_CONTRACT.md`

Für Issue #24 ist in der ersten Implementierung umgesetzt:

- `GET /api/health`
- `GET /api/topics`
- `GET /api/topics/:id`
